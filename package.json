{
  "name": "nba-stats-app",
  "version": "1.0.0",
  "description": "NBA Stats Application",
  "scripts": {
    "postinstall": "npm install --prefix ./Application/frontend && npm run build --prefix ./Application/frontend",
    "start": "node server.js"
  },
  "dependencies": {}
}