{
  "name": "nba-stats-app",
  "version": "1.0.0",
  "description": "NBA Stats Application",
  "scripts": {
    "postinstall": "cd Application/frontend && npm install && npm run build && cd ../backend && pip install -r requirements.txt",
    "start": "node server.js"
  },
  "dependencies": {}
}